# Provenance And Supply Chain Verification

This repository is generated from `reponomics-dashboard-dev` and consumes the versioned `reponomics-dashboard-action` runtime.

## Practical Verification

1. Inspect workflow refs in `.github/workflows/`.
2. Confirm the action release in workflow metadata and run logs.
3. Verify collection and publish provenance artifacts in workflow runs.
4. For released template updates, compare generated changes against source release notes.

No scanner or control can guarantee perfect security. Provenance checks reduce risk; they do not remove it.
