name: DEV / CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install maintainer dependencies
        run: pip install -r requirements-dev.txt

      - name: Verify workflow classification
        run: python scripts/verify_workflow_classification.py

      - name: Run test suite
        env:
          PYTHONPATH: scripts
        run: python -m pytest tests/ -v
